{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "checkout": "yarn workspace checkout",
    "host": "yarn workspace host",
    "product": "yarn workspace product",
    "postinstall": "yarn lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
